
// Firebase has been removed.
// This file is kept empty to prevent import errors during transition if any remains,
// but all imports should be removed.
export const auth = null;
export const db = null;
